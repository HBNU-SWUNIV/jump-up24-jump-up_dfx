const btn_download = document.getElementById("downloader");
const urlParams = new URL(location.href).searchParams;

const code = urlParams.get('code');

console.log(name)
btn_download.addEventListener("click", (e) => {
})